{
  "name": "dhpham-website",
  "version": "1.0.0",
  "packageManager": "pnpm@10.33.4",
  "description": "",
  "scripts": {
    "test": "vitest run",
    "test:all": "pnpm test && pnpm run test:quartz:check && pnpm run test:quartz && pnpm run test:artifacts && pnpm run test:e2e",
    "test:e2e": "pnpm run build && PLAYWRIGHT_PORT=${PLAYWRIGHT_PORT:-$((30000 + $$ % 10000))} playwright test",
    "test:quartz": "corepack pnpm --dir blog/quartz test",
    "test:quartz:check": "corepack pnpm --dir blog/quartz check",
    "test:artifacts": "pnpm run build:fixture && pnpm run verify:fixture && pnpm run build && pnpm run verify:production",
    "verify:fixture": "node tests/verify-generated-output.mjs fixture",
    "verify:production": "node tests/verify-generated-output.mjs production",
    "build": "rm -rf dist && pnpm run build:home && pnpm run build:blog",
    "build:home": "tsc && vite build",
    "build:blog": "corepack pnpm --dir blog/quartz quartz build --directory content --output ../../dist/blog",
    "build:fixture": "rm -rf .test-artifacts/quartz-fixture && corepack pnpm --dir blog/quartz quartz build --directory ../../tests/fixtures/quartz-content --output ../../.test-artifacts/quartz-fixture",
    "preview": "vite preview",
    "start": "tsc --watch& vite"
  },
  "engines": {
    "node": "24.x",
    "npm": ">=10.x",
    "pnpm": ">=10.x"
  },
  "keywords": [],
  "author": "dayvidpham@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/three": "^0.184.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.13",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
